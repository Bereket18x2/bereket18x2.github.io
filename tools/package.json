{
  "name": "finote-yared-tools",
  "private": true,
  "type": "module",
  "description": "Local operator scripts. NEVER imported by a page — see CLAUDE.md on the npm exception.",
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore --project finote-yared \"node rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase-tools": "^13.29.1"
  }
}
